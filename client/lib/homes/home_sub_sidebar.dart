import 'package:flutter/material.dart';

class SubSidebr extends StatelessWidget {
  const SubSidebr({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text("sidebar"),
    );
  }
}