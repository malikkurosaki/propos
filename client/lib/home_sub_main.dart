import 'package:flutter/material.dart';

class HomeSubMain extends StatelessWidget {
  const HomeSubMain({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Home Sub Main"),
    );
  }
}